/**
 * Enumeration of Session Attributes that can be set by system at session startup
 */
enum Spark.Core.Manager.SessionAttributeEnum:

    define enum
                timezoneOffset
                dateFormat
                numericDecimalPoint
                numericSeparator
                .
end enum.
